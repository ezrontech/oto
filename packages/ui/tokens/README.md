Design tokens (colors, radii, font sizes) generated from `Oto AI Agent Overview/src/styles/theme.css`.

Files:
- `tokens.json` — canonical token values (source of truth)
- `tokens.css` — CSS variables for runtime
- `tokens.ts` — TypeScript export for use in components

Next steps:
- Import `tokens.css` from your app entry (`_app.tsx` or root layout) to expose CSS variables.
- Replace hard-coded values across components with token references.

Design source (copied for Phase 1): ../../designs/figma
